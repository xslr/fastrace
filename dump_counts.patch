--- cpp/src/Analyzer.cpp
+++ cpp/src/Analyzer.cpp
@@ -1341,2 +1341,8 @@
     totalMessages_ = cumulativeMessages;
+    
+    FILE* f = fopen("build_index_counts.txt", "w");
+    for (const auto& cc : containerCounts) {
+        fprintf(f, "%u\n", cc.countInContainer);
+    }
+    fclose(f);
+
     return totalMessages_;
@@ -1429,2 +1435,5 @@
                 tailFrag = std::move(nextTailFrag);
+                
+                // We can't easily dump decodeChunk counts here without breaking the chunk boundaries, 
+                // because decodeChunk is called 400 times.
             }
